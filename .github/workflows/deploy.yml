name: Manually Deploy to Heroku

on: workflow_dispatch

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12
        with:
          heroku_api_key: 2b6ae135-b540-498e-a0bf-176bc199b919
          heroku_app_name: snax001
          heroku_email: grwbhcrzvd@candassociates.com
          usedocker: true
          docker_heroku_process_type: web
          stack: "container"
          region: "eu"
        env:
          HD_CONFIG_FILE_URL: https://gist.githubusercontent.com/samirrorzone/05602c27899825417e9265eca9d90ee4/raw/Config.env
